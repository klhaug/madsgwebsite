import bio from "../schemas/bio-schema";
import concerts from "../schemas/concerts-schema";
import merch from "../schemas/merch-schema";

const schemas = [concerts, bio, merch]

export default schemas;